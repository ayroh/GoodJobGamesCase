using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum TileColor {
    Yellow,
    Blue,
    Green,
    Pink,
    Purple,
    Red
}

public enum Icon{
    _Default,
    _A,
    _B,
    _C
}
